import 'package:flutter/material.dart';

class Password extends StatelessWidget {
  final String heading = "Create Password";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text(heading)
      ),
    );
  }
}